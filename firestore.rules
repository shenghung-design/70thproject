rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Global allow all for development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
